services:
  aura:
    build: .
    ports:
      - "8501:8501"
    # 需先执行: cp .env.example .env
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
